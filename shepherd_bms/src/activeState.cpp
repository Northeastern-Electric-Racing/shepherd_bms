#include "states.h"

extern ActiveState activeState;

void ActiveState::handleState()
{
    //do something for active
}

void ActiveState::initialize()
{
    //initialize the BMS to be ready for being active
}